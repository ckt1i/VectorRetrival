================================================================================
UNDO.txt — Deferred Optimizations & Known Limitations
================================================================================

This file tracks intentional simplifications and planned optimizations.
Each entry describes what was deferred, why, and how to implement it.

================================================================================
Phase 5 — IO Optimization (Single-Query Focus)
================================================================================
Target hardware: Kioxia KCD61LUL3T84 NVMe (3.5TB, 1M IOPS, ~6.9 GB/s seq read)
Server kernel: 6.8.0-87-generic (supports DEFER_TASKRUN)
NVMe params: max_hw_sectors_kb=128, max_segments=33, nr_requests=1023
Reference docs: IO优化方案.md, 优化技巧.md

--------------------------------------------------------------------------------
[PHASE5-001] DEFER_TASKRUN + SINGLE_ISSUER (Path A: io_uring flags)
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : HIGH — 3 lines, zero risk, immediate benefit
Files    : src/query/io_uring_reader.cpp (Init)

What to do:
  - Add IORING_SETUP_SINGLE_ISSUER | IORING_SETUP_DEFER_TASKRUN to params.flags
  - Add fallback: if io_uring_queue_init_params fails, retry without these flags
    (for kernels < 5.19)

Benefit: p99 tail latency stabilized — eliminates random kernel preemption of
  CPU-intensive ProbeCluster computations.

Constraint: SINGLE_ISSUER limits the ring to the creating thread only.
  Compatible with current single-threaded model. Future multi-thread needs
  one ring per thread.

--------------------------------------------------------------------------------
[PHASE5-002] Registered File Descriptors (Fixed Files)
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : LOW — ~15 lines, minor benefit (~80μs/query)
Files    : src/query/io_uring_reader.cpp, include/vdb/query/async_reader.h

What to do:
  - After Init(): io_uring_register_files(&ring, fds, 2) for clu_fd + dat_fd
  - PrepRead: sqe->flags |= IOSQE_FIXED_FILE, use fd_index instead of fd
  - Saves fget/fput atomic ops per SQE (~200ns × ~756 SQE = 151μs/query)

--------------------------------------------------------------------------------
[PHASE5-003] .clu File Layout 4KB Alignment (Path B: O_DIRECT prerequisite)
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : HIGH — required for O_DIRECT
Files    : src/storage/cluster_store.cpp (Writer side)

What to do:
  - BeginCluster/EndCluster: pad each block to 4KB boundary
  - Lookup table offset/size reflect aligned values
  - Add file format version number for compatibility

Breaking change: old .clu files incompatible, need index rebuild.
Space overhead: ≤ nlist × 4095B ≈ 1MB (<1% of .clu total).

Note: .dat is ALREADY 4KB-aligned (page_size=4096 padding). No changes needed.

--------------------------------------------------------------------------------
[PHASE5-004] Buffer Allocation Alignment
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : HIGH — required for O_DIRECT
Files    : src/query/overlap_scheduler.cpp (SubmitClusterRead)
           include/vdb/query/buffer_pool.h, src/query/buffer_pool.cpp

What to do:
  - Cluster block: change `new uint8_t[size]` → `aligned_alloc(4096, round_up(size, 4096))`
  - BufferPool: internal alloc → aligned_alloc(4096, ...), free() instead of delete[]
  - Check nullptr return from aligned_alloc (returns nullptr on failure, not exception)

--------------------------------------------------------------------------------
[PHASE5-005] O_DIRECT Toggle
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : MEDIUM — needs PHASE5-003 + PHASE5-004 first
Files    : src/storage/cluster_store.cpp, src/storage/data_file_reader.cpp

What to do:
  - Add SearchConfig.use_direct_io = false
  - open() with O_DIRECT when enabled
  - Strategy: small dataset (<50% RAM) → buffered IO; large dataset → O_DIRECT

Dependency chain: PHASE5-003 (.clu align) + PHASE5-004 (buffer align) → PHASE5-005

--------------------------------------------------------------------------------
[PHASE5-006] IOPOLL (Conditional — needs cold-read benchmark)
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : LOW — only valuable when IO-bound (data >> RAM)
Files    : src/query/io_uring_reader.cpp

What to do:
  - Add IORING_SETUP_IOPOLL to params.flags
  - Requires O_DIRECT (PHASE5-005)

Caution: Current workload is CPU-bound (io_wait=0.016%). IOPOLL's busy-wait
  may waste CPU. Only beneficial for cold-data scenarios. Benchmark first:
    echo 3 > /proc/sys/vm/drop_caches → measure real io_wait

--------------------------------------------------------------------------------
[PHASE5-007] Lookup Table Bulk Pread
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : MEDIUM — mandatory under O_DIRECT (small preads violate alignment)
Files    : src/storage/cluster_store.cpp (ClusterStoreReader::Open)

What to do:
  - Replace ~2000 per-field PreadValue calls with single pread of entire
    lookup table (nlist × entry_size), then parse in memory
  - Saves ~4ms startup time; becomes REQUIRED under O_DIRECT

--------------------------------------------------------------------------------
[PHASE5-008] pending_ HashMap → Slot Vector
--------------------------------------------------------------------------------
Status   : Not implemented
Priority : LOW — ~30 lines, ~160μs/query saving
Files    : include/vdb/query/overlap_scheduler.h, src/query/overlap_scheduler.cpp

What to do:
  - Encode PendingIO slot index into io_uring user_data (64-bit)
  - Replace unordered_map<uint8_t*, PendingIO> with vector<PendingIO> + freelist
  - Eliminates hash lookup on every CQE dispatch

--------------------------------------------------------------------------------
[PHASE5-009] IO Size Awareness: max_segments=33 → 4.1MB Threshold
--------------------------------------------------------------------------------
Status   : Not implemented (currently not needed)
Priority : LOW — only relevant at 100M+ scale

Note: This server's max_segments=33 means the io_uring async worker slow path
  triggers at 33 × 128KB = 4.1MB (NOT the 15.9MB assumed in IO优化方案.md).
  Current max cluster block = 1.1MB (100K scale) → safe.
  At 100M scale (avg=2.7MB, some blocks may exceed 4.1MB), need user-space
  splitting in PrepRead: split large IO into ≤4MB sub-SQEs, aggregate CQEs
  internally, report single completion to OverlapScheduler.

================================================================================
Phase 6 — Coroutine/Fiber Architecture (DEFERRED)
================================================================================

--------------------------------------------------------------------------------
[PHASE6-001] Fiber-Based Query Scheduler (boost.fcontext)
--------------------------------------------------------------------------------
Status   : Not implemented — deferred until multi-query/batch query phase
Priority : FUTURE — architectural change, ~700-900 lines

Why deferred:
  - Current single-query pipeline is 100% CPU-bound (io_wait=0.016%)
  - overlap_ratio=99.98% — manual event loop already achieves near-perfect overlap
  - Fiber yield/resume overhead (~1-5μs) × nprobe × yields would be non-trivial
  - Coroutines solve "IO wait while CPU idle" — a problem that doesn't exist yet

When to implement:
  - When implementing batch/concurrent query execution (multiple queries sharing
    worker threads)
  - When data exceeds RAM and IO becomes a real bottleneck

Architecture (from 优化技巧.md):
  1. Per-CPU worker thread, each with 1 io_uring ring + 1 FiberScheduler
  2. Each query = fiber. ProbeCluster becomes fiber function with yield on IO
  3. Scheduler main loop: run_ready_fibers → submitIO (batch) → poll CQE → wake
  4. Multi-query IO requests aggregate into large batches → saturate NVMe IOPS
  5. No locks needed (fibers within same thread are cooperative)

Implementation layers:
  Layer 1: CMake + Boost.Context dependency (~10 lines)
  Layer 2: FiberScheduler class (ready queue, IO wait table, main loop) ~200-300 lines
  Layer 3: Rewrite OverlapScheduler as fiber functions ~300-400 lines
  Layer 4: Multi-thread dispatch (round-robin or work-stealing) ~200 lines

Alternative: C++20 coroutines (co_await) — zero overhead but more complex to write.
  Boost.fcontext preferred for stackful semantics and simpler migration from
  current explicit state machine.

================================================================================
Phase 3 — SIMD & Codec Deferred Items
================================================================================

--------------------------------------------------------------------------------
[PHASE3-002] bit_unpack: Only 1-bit Has AVX2/AVX-512 Specialization
--------------------------------------------------------------------------------
Status  : Partially specialized
Files   : include/vdb/simd/bit_unpack.h, src/simd/bit_unpack.cpp

What was done:
  - bit_width=1 has a dedicated AVX2 path: broadcasts each byte to a __m256i,
    uses _mm256_srlv_epi32 with shift vector [0..7], AND with 1, then stores
    8 uint32_t per byte. This is the critical path for RaBitQ 1-bit quantization.
  - bit_width=2, 4, 8 fall through to the scalar BitUnpackScalar path.

Why deferred:
  - RaBitQ primarily uses 1-bit quantization; 2/4/8-bit are lower priority.
  - Scalar is correct and readable; performance profiling should guide when to
    add AVX2 paths for other widths.

How to implement later (per bit_width):
  bit_width=2 (count=64, 16 bytes → 64 uint32_t):
    - Load 2 bytes at a time, broadcast to __m256i, use shift vector [0,2,4,6,0,2,4,6]
      and mask 0x3 to extract 4 2-bit values per register pair.
    - Or: process 4 bytes → 16 uint32_t per __m256i using _mm256_srlv_epi32 with
      shift vector [0,2,4,6,0,2,4,6] and value broadcast.

  bit_width=4 (count=64, 32 bytes → 64 uint32_t):
    - Use _mm256_loadu_si256 to load 32 bytes, then _mm256_and_si256 with 0x0f0f0f0f
      mask to extract low nibbles, and >>4 for high nibbles.
    - _mm256_unpacklo_epi8 / _mm256_unpackhi_epi8 + zero-extend to uint32_t.

  bit_width=8 (count=64, 64 bytes → 64 uint32_t):
    - Simple zero-extension: load 8 bytes as __m64 or 16 bytes as __m128i,
      use _mm256_cvtepu8_epi32 (8 bytes → 8 uint32_t per call) × 8 iterations.

--------------------------------------------------------------------------------
[PHASE3-003] prefix_sum: AVX2 Processes 8 uint32_t/iter, No 16/32-wide Path
--------------------------------------------------------------------------------
Status  : Implemented (AVX2, 8 uint32_t/iter)
Files   : include/vdb/simd/prefix_sum.h, src/simd/prefix_sum.cpp

What was done:
  - AVX2 path processes 8 uint32_t per __m256i register using _mm256_slli_si256
    (intra-lane shift) and _mm_alignr_epi8 for the cross-lane carry.
  - Scalar tail for remainder (count % 8 != 0).

Potential optimization:
  - Loop unrolling: process 2 × __m256i per iteration to reduce loop overhead
    for count=64 (8 iterations → 4 iterations).
  - AVX-512 path: _mm512_alignr_epi32 can do cross-lane shifts natively for
    16 uint32_t per register, handling count=64 in 4 iterations.
  - For very large count (> 1024), a parallel prefix sum using reduction trees
    would be more cache-efficient.

--------------------------------------------------------------------------------
[PHASE3-004] BitpackCodec::Encode: Scalar Bit-Packing (No SIMD Write Path)
--------------------------------------------------------------------------------
Status  : Scalar implementation
Files   : include/vdb/codec/bitpack_codec.h, src/codec/bitpack_codec.cpp

What was done:
  - Encode() writes bit_width bits per value into a packed byte array using
    scalar OR + shift operations (bit_pos / 8, bit_pos % 8 per element).
  - Decode() delegates to vdb::simd::BitUnpack (AVX2 for 1-bit, scalar for rest).

Why deferred:
  - Encoding is performed offline during index construction (not on the query
    critical path), so scalar speed is acceptable.

How to optimize later:
  - For bit_width=1: process 32 values at a time using AVX2 _mm256_movemask_epi8
    after comparing each 32-element batch with a mask. Store as a single
    uint32_t (one byte per iteration with _mm_movemask_epi8 on __m128i).
  - For bit_width=4: pack pairs of nibbles using _mm256_packs_epi16 +
    shuffle, then store 32 bytes.
  - For bit_width=8: trivially memcpy.

--------------------------------------------------------------------------------
[PHASE3-005] DictCodec: Simple std::unordered_map — No Compression of Indices
--------------------------------------------------------------------------------
Status  : Simple implementation
Files   : include/vdb/codec/dict_codec.h, src/codec/dict_codec.cpp

What was done:
  - BuildDict() uses std::unordered_map<string, uint32_t> for O(n) dict build;
    insertion order defines dict entry ordering.
  - Indices are stored as uint32_t (not further compressed).
  - Decode() is a simple lookup into the dict vector.

Potential future optimizations:
  1. Sort the dict by frequency and use smaller index types (uint8_t if dict
     size ≤ 256, uint16_t if ≤ 65536) to reduce storage. Pair with
     BitpackCodec to pack the indices.
  2. Integrate with BitpackCodec: after BuildDict(), call
     ComputeMinBitWidth(indices) and Encode(indices) to get a compact byte
     stream for on-disk storage of payload columns.
  3. For very large datasets, use a perfect hash (e.g. gperf) to minimize
     decode lookup time.
  4. The current BuildDict preserves insertion order. If sorted order is
     needed (for binary search decode), sort dict by value or by frequency.

--------------------------------------------------------------------------------
[PHASE3-006] distance_l2: Unaligned Loads Only
--------------------------------------------------------------------------------
Status  : Uses _mm256_loadu_ps (unaligned)
Files   : src/simd/distance_l2.cpp

What was done:
  - Uses _mm256_loadu_ps for both input vectors regardless of alignment.

Potential optimization:
  - When both pointers are 32-byte aligned (VDB_SIMD_ALIGNED), use
    _mm256_load_ps for slightly better throughput on older hardware.
  - In practice, modern CPUs (Haswell+) have negligible penalty for unaligned
    loads when data is actually aligned. Only worth adding if profiling shows
    a measurable bottleneck.
  - Could add a fast path: if (((uintptr_t)a | (uintptr_t)b) & 31) == 0,
    use _mm256_load_ps, otherwise _mm256_loadu_ps.

--------------------------------------------------------------------------------
[PHASE3-007] No Runtime CPU Feature Detection (Compile-Time Dispatch Only)
--------------------------------------------------------------------------------
Status  : Compile-time dispatch via #ifdef VDB_USE_AVX2
Files   : All simd/*.cpp

What was done:
  - SIMD path selection happens at compile time via CMake options and
    corresponding preprocessor defines (VDB_USE_AVX2=1, VDB_USE_AVX512=1).
  - A single binary cannot run both AVX2 and scalar paths at runtime.

Why deferred:
  - Runtime dispatch adds code complexity (function pointer tables, CPUID
    detection, initialization overhead).
  - The current build target is a server with known CPU capabilities.

How to implement later:
  1. Use __builtin_cpu_supports("avx2") at runtime to select paths.
  2. Create separate compilation units with different -march flags and
    use function multiversioning (__attribute__((target("avx2")))).
  3. Or use a dispatch library like highway (google/highway) which handles
    this automatically.

================================================================================
Phase 4 Deferred Optimizations & Known Limitations (RaBitQ)
================================================================================

--------------------------------------------------------------------------------
[PHASE4-001] Rotation Matrix: O(L²) Apply — Hadamard FWHT O(L log L) available
--------------------------------------------------------------------------------
Status  : Implemented (both paths available)
Files   : include/vdb/rabitq/rabitq_rotation.h, src/rabitq/rabitq_rotation.cpp

What was done:
  - Full random orthogonal matrix via Gram-Schmidt QR is implemented.
  - Hadamard mode (P = (1/√L) H × D) is implemented with both:
    (a) O(L²) explicit matrix multiply path (use_fast_transform=false)
    (b) O(L log L) fast Walsh-Hadamard transform path (use_fast_transform=true)
  - Hadamard requires dim to be a power of 2.

Future optimization:
  1. SIMD-accelerate the Apply() matrix-vector multiply for the random
     orthogonal case (AVX2 FMA in chunks of 8).
  2. For Hadamard FWHT, implement AVX2-accelerated butterfly operations.
  3. Consider generating P once and serializing to disk (not regenerating
     per-index-load).

--------------------------------------------------------------------------------
  [REMOVED] PHASE4-002: Multi-bit RaBitQ — IMPLEMENTED as ExRaBitQ
    (ip_exrabitq.cpp, parsed_cluster.h ex_code/ex_sign, overlap_scheduler has_s2_)
  [REMOVED] PHASE4-003: AVX-512 VPOPCNTDQ — IMPLEMENTED in popcount.cpp
    (_mm512_popcnt_epi64)
  [REMOVED] PHASE3-001: AVX-512 paths — IMPLEMENTED in distance_l2, popcount,
    ip_exrabitq, fastscan. (bit_unpack/prefix_sum still lack AVX-512, tracked
    in PHASE3-002 and PHASE3-003 respectively)
--------------------------------------------------------------------------------
[PHASE4-004] RaBitQ Encoder: Per-vector Heap Allocation
--------------------------------------------------------------------------------
Status  : Known inefficiency
Files   : src/rabitq/rabitq_encoder.cpp

What was done:
  - Encode() allocates two std::vector<float> (residual, rotated) per call.
  - EncodeBatch() calls Encode() in a loop.

How to implement later:
  1. Pre-allocate thread-local scratch buffers.
  2. EncodeBatch() should share scratch across iterations.
  3. Consider AlignedBuffer<float> for SIMD-aligned temporaries.

--------------------------------------------------------------------------------
[PHASE4-005] Gram-Schmidt Numerical Stability for Large dim
--------------------------------------------------------------------------------
Status  : Acceptable for dim ≤ 256; may degrade for dim ≥ 512
Files   : src/rabitq/rabitq_rotation.cpp

What was done:
  - Modified Gram-Schmidt (row-oriented) for QR decomposition.
  - Orthogonality verified up to dim=128 with tolerance 1e-3.

How to implement later:
  1. Use Householder reflections for better numerical stability.
  2. Or use LAPACK dgeqrf/sgeqrf if available.
  3. Or apply re-orthogonalization pass after initial Gram-Schmidt.

================================================================================
End of UNDO.txt
================================================================================
