# 实验计划

**问题**：在完成同步暖服务调优、`.clu` 全量预加载、`bits=4` 重建校正和 `epsilon=0.90, nprobe=512` 主曲线刷新后，BoundFetch 已在 COCO 100K 上进入 `0.97-0.99` 高召回区间。当前需要继续优化方法本身，在不改变搜索语义的前提下进一步压缩 query-prep / probe 的 CPU 成本。
**方法论文**：当前工作流的主线已经收束为“在固定的 baseline 证据输入下，对 BoundFetch 做有限轮次的 CPU 热路径优化”。baseline 实验改由并行任务维护；本计划只覆盖方法继续优化、复测和后续扩展验证。
**日期**：2026-04-16

## 当前分工

- **本任务负责**：BoundFetch 方法继续优化，重点是 query-prep / probe CPU 热路径。
- **并行任务负责**：继续运行、补齐和维护所有 baseline 实验及其图表/表格。
- **本计划中的 baseline 相关内容**：只作为外部输入和对照条件保留，不再作为本任务的执行清单。

## 声明映射表

| 声明 | 重要性 | 最小可信证据 | 关联块 |
|-------|----------------|-----------------------------|---------------|
| C1 | 暖驻 `.clu` 聚类侧状态预加载是有效优化，但已不再是主要缺失杠杆。 | 预加载保持召回语义不变并改善延迟，但在高召回区间与 DiskANN 的差距仍然存在。 | B1, B2 |
| C2 | BoundFetch 下一步最重要的优化目标应是 query-prep / probe 的 CPU 热路径，而不是继续做 cluster submit-path 微调。 | R067 和后续 corrected `bits=4` sweep 表明 `.clu` I/O 与 rerank 都不是主要瓶颈，热点集中在 `QuantizeQuery14Bit`、`PrepareQueryRotatedInto` 与 probe 本身。 | B3 |
| C3 | baseline 结果应作为固定输入支撑当前方法优化，而不是继续占用本任务的主要执行预算。 | 现有主曲线已经足够说明 BoundFetch 的当前位置；后续 baseline 扩展由并行任务维护，本任务只消费这些结果。 | B4, B5, B6 |

## 论文故事线
- 主论文必须证明：
  - 暖稳态 E2E 服务仍然是正确的评估目标
  - `.clu` preload 是已经完成且有效的优化，而不是最终答案
  - 下一步有意义的 BoundFetch 优化是 query-prep / probe 的 CPU 侧效率，而非更多盲目的 `io_uring` 微调
  - 在固定 baseline 结果输入下，BoundFetch 的下一步提升应来自方法本身，而不是更多对比实验
  - DiskANN 与 IVF 家族结果保留为外部参考，但不再是本任务的执行主体
- 附录可支撑：
  - 额外的存储后端变体
  - 残余队列深度或模式敏感性检查
  - 图基线参考数据
  - 在 Deep1M / Deep8M 以及一个真实数据负载上的通用性
- 有意裁剪的实验：
  - 冷启动 / drop-cache 协议
  - 完整的 `IVF+PQ/RQ/(DiskANN) x Lance/Parquet/(FlatStor)` 笛卡尔基准测试
  - 在预加载结果明确前又一轮提交路径调优

## 基线对比策略

新基线策略是有意分层的。

- 即使 DiskANN 当前主导暖前沿，也保留它在报告的对比中。
- 不要将 `FlatStor` 作为最终故事中唯一的存储后端。
- 不要尝试完整的搜索后端笛卡尔积，除非自动化使其几乎免费。
- 使用三层对比代替：
  - **层 A：主搜索核心对比**
    - BoundFetch
    - IVF+PQ / IVF+RQ / ConANN 等 IVF 家族方法
    - 若后端策略足够对齐，可附带 DiskANN 概览行
    - 一个选定的存储后端或一个明确记录的后端策略
  - **层 B：存储后端消融**
    - 一到两个代表性搜索核心
    - `FlatStor`、`Lance`，可选 `Parquet`
  - **层 C：构建 / 预加载成本表**
    - 构建时间
    - 索引大小
    - 驻留预加载大小
    - 启动预加载时间

推荐解释：
- 如果对齐的 `Lance` 集成对所有比较系统都可行，优先选择 `Lance` 作为面向生产的主后端。
- 如果对齐后端集成成本太高，保持 `DiskANN+FlatStor` 作为强暖服务上界参考，并将后端格式对比放入层 B。
- 主声明应先在 IVF 家族内部建立，因为这更符合 BoundFetch 的快速构建与未来动态更新定位。
- `Parquet` 只应在工程成本较小时添加；否则归入附录或未来工作。

## 实验块

### 块 1：暖驻聚类预加载
- 测试声明：C1
- 为什么存在此块：这是直接针对当前 DiskANN 差距的最小结构性改变。
- 数据集/分割/任务：`coco_100k`，top-k=10，暖稳态服务，`queries=1000`
- 对比系统：
  - BoundFetch `preload=off`
  - BoundFetch `preload_codes`
  - BoundFetch `preload_codes_and_addr`
- 指标：
  - 主要：`recall@10`、`e2e_ms`、`p99_ms`
  - 次要：`uring_submit_ms`、`probe_ms`、驻留 RSS、预加载时间
- 设置细节：
  - 从当前最佳已知工作点开始：`nlist=2048, nprobe=200, crc-alpha=0.05, epsilon=0.99`
  - 不预加载载荷体
  - 记录驻留内存增长和启动预加载持续时间
- 成功标准：
  - 预加载减少 E2E 延迟或聚类侧时间，足够将 BoundFetch 前沿移向 DiskANN
  - 召回率在相同搜索设置下保持不变
- 失败解释：
  - 如果预加载效果不大，剩余差距可能是搜索质量或算法性的而非热路径元数据访问
- 表格/图形目标：
  - 主论文或附录消融表
  - 分解图显示预加载后什么移动了
- 优先级：必须运行

### 块 2：预加载后刷新的 BoundFetch 帕累托
- 测试声明：C1
- 为什么存在此块：一次预加载消融不够；需要看整个 BoundFetch 前沿是否移动。
- 数据集/分割/任务：`coco_100k`，top-k=10，暖稳态服务，`queries=1000`
- 对比系统：
  - BoundFetch 当前前沿
  - BoundFetch 启用预加载
- 指标：
  - `recall@10`
  - `e2e_ms`
  - `p99_ms`
- 设置细节：
  - 在 `crc-alpha={0.01,0.02,0.05,0.08,0.1}` 上重新运行紧凑网格，`nprobe={200,500}`
  - 只有在预加载实质性改变前沿形状时才添加 `nlist` 或 `epsilon` 细化
- 成功标准：
  - 至少出现一个新的非支配 BoundFetch 点
- 失败解释：
  - 如果刷新的前沿仍完全被 DiskANN 支配，停止低级调优并将论文重新定位到构建时间/集成权衡
- 表格/图形目标：
  - 更新的主帕累托图
- 优先级：必须运行

### 块 3：BoundFetch 的 CPU 侧优化
- 测试声明：C2
- 为什么存在此块：在 preload 完成后，剩余差距最可能来自 query-prep / probe 的 CPU 成本，尤其是在 `recall@10 = 0.98` 左右的高召回区间。
- 数据集/分割/任务：`coco_100k`，top-k=10，暖稳态服务，`queries=1000`
- 对比系统：
  - BoundFetch 当前最佳 preload 设置
  - 每次只引入一个 CPU 路径优化后的 BoundFetch
- 指标：
  - `recall@10`
  - `e2e_ms`
  - `p99_ms`
  - `probe_ms`
  - `rerank_cpu_ms`
  - 扫描/验证候选数
- 设置细节：
  - 固定 `clu_mode=full_preload`
  - 以当前主曲线的两个高价值工作点为锚点：
    - `nlist=2048, nprobe=512, bits=4, epsilon=0.90, alpha=0.02`
    - `nlist=2048, nprobe=512, bits=4, epsilon=0.90, alpha=0.01`
  - 仅实现那些能在不改变搜索语义下减少 query-prep 或 per-cluster probe CPU 开销的改动
  - 候选方向包括：
    - 优化 query buffer / probe 数据布局和 SIMD 友好性
    - 减少 probe 路径中的重复 decode / bound-check
    - 仅在前两项完成后，才考虑 candidate materialization 与 rerank
  - 已知应停止的方向：
    - `fastscan-lut-fusion-optimization`：两版实现均未通过 benchmark gate，第二版 fused helper 更慢，代码已回退
- 成功标准：
  - 至少有一个 `recall@10 >= 0.98` 的工作点在不改变召回语义下明显向 DiskANN 靠近
- 失败解释：
  - 如果 CPU 侧调优无法实质推动 `>=0.98` 区间，就停止继续追逐 DiskANN 的暖延迟
- 表格/图形目标：
  - 机制表 + 刷新的高召回 Pareto
- 优先级：必须运行

### 块 4：同步 IVF 家族基线（并行任务维护）
- 测试声明：C3
- 为什么存在此块：在继续追 DiskANN 之前，需要先证明 BoundFetch 在它最自然的同类方法中处于什么位置。
- 数据集/分割/任务：`coco_100k`，代表性暖服务工作点
- 对比系统：
  - BoundFetch 最佳 preload 点
  - FAISS IVF+PQ
  - 若可用，再补一个 IVF 家族基线，优先 IVF+RQ 或 ConANN
- 指标：
  - `recall@10`
  - `e2e_ms`
  - `p99_ms`
  - 构建时间
- 设置细节：
  - 在相同暖协议下，将每个 baseline 都调成自己的 Pareto 曲线
  - 尽量统一 `queries=1000`、`topk=10`，以及可比时统一 `nlist`
  - 形成一张同步图，至少包含：
    - BoundFetch
    - IVF+PQ
    - IVF+RQ / ConANN
- 成功标准：
  - 建立 BoundFetch 在 IVF 家族 tradeoff surface 上的领先位置
- 失败解释：
  - 如果另一个 IVF 家族方法支配 BoundFetch，应先解释并修复这一差距，再考虑扩数据集
- 表格/图形目标：
  - 主 Pareto 图中的 IVF 家族对比
- 优先级：并行任务

### 块 5：DiskANN 参考与成本表（并行任务维护）
- 测试声明：C3
- 为什么存在此块：DiskANN 应该继续保留，但更适合作为强图算法参考，再配合构建/启动成本形成完整权衡。
- 数据集/分割/任务：`coco_100k`
- 对比系统：
  - BoundFetch
  - DiskANN
  - 一个调优后的 IVF 家族基线
- 指标：
  - `recall@10`
  - `e2e_ms`
  - `p99_ms`
  - 构建时间
  - 构建期间峰值 RSS
  - 索引字节
  - 预加载字节
  - 预加载时间
- 设置细节：
  - 将这些指标与查询延迟分开报告
  - 将构建和预加载成本作为一级证据，而非脚注
- 成功标准：
  - 产生一张清晰表格，显示即使 DiskANN 在服务上更快，BoundFetch 是否仍占据更好的构建时间/启动/简单性点
- 失败解释：
  - 如果 BoundFetch 在构建和启动成本上也输了，论文范围必须再次缩小
- 表格/图形目标：
  - 主成本表 + 次级 DiskANN 参考图
- 优先级：并行任务

### 块 6：存储后端消融（并行任务维护）
- 测试声明：C3
- 为什么存在此块：必须把搜索核心效应和后端格式效应拆开，否则结论会显得混淆。
- 数据集/分割/任务：`coco_100k`，代表性暖服务工作点
- 对比系统：
  - 一到两个代表性搜索核心跨 `FlatStor`、`Lance`，可选 `Parquet`
- 指标：
  - `recall@10`
  - `e2e_ms`
  - 启动时间
  - 文件大小
- 设置细节：
  - 默认不运行完整矩阵
  - 从 BoundFetch 和一个 IVF 家族基线开始
  - 只有在后端集成现成时才添加 DiskANN
- 成功标准：
  - 存储格式贡献可见，但不淹没主故事
- 失败解释：
  - 如果代价过高，缩成 `FlatStor` vs `Lance` 或移到附录
- 表格/图形目标：
  - 附录存储后端表
- 优先级：并行任务

### 块 7：超出 COCO 100K 的通用性检查
- 测试声明：仅支撑证据
- 为什么存在此块：一旦主比较故事稳定，论文不应继续停留在 COCO-only。
- 数据集/分割/任务：
  - 第一阶段：`deep1m` 或 `deep8m`
  - 第二阶段：一个真实数据负载，优先 `MS MARCO Passage` 或 `Amazon Products`
- 对比系统：
  - BoundFetch 最佳点
  - 最强的调优 IVF 家族基线
  - 可选的 DiskANN 参考点
- 指标：
  - 召回率趋势
  - 延迟趋势
  - 构建 / preload 趋势
- 设置细节：
  - 只在块 1-5 稳定后才运行
  - 先做合成/标准向量规模扩展，再做真实数据异质性验证
- 成功标准：
  - 说明当前方向不是 COCO 独有现象
- 失败解释：
  - 若延误主故事，则移至附录或裁剪
- 表格/图形目标：
  - 附录支撑图表
- 优先级：可选

## 运行顺序与里程碑

| 里程碑 | 目标 | 运行 | 决策门控 | 成本 | 风险 |
|--------|------|------|---------------|------|------|
| M0 | 在 preload 和重测后重新锚定文档 | 更新报告、计划和追踪器 | 所有未来运行使用新的 post-preload 策略 | 0.5 天 | 低 |
| M1 | 将 preload 视为已完成优化并封板 | 块 1 和 2 | 除非出现新证据，否则不再重启 submit-path 调优 | 0.5-1 天 | 低 |
| M2 | 判断 CPU 调优是否仍值得继续 | 块 3 | `fastscan-lut-fusion-optimization` 已失败并回退；后续仅在提出新 CPU 假设时继续 | 1-3 天 | 中 |
| M3 | 消费并行 baseline 结果，维护主比较结论 | 块 4 和 5 | baseline 结果由并行任务提供，本任务不阻塞其执行 | 0.5-1 天 | 低 |
| M4 | 仅在方法线稳定后再做扩展验证 | 块 7 | 只在 M1-M3 已稳定后运行 | 2-4 天 | 中 |

## 计算与数据预算
- 预计 GPU 小时：0
- 数据准备需求：
  - 无需新的 COCO 准备
  - 可选的 `Lance` 或 `Parquet` 后端转换工作
- 人工评估需求：无
- 最大瓶颈：工程时间，而非计算
- 最大协调规则：保持实验套件分层；不让搜索后端矩阵爆炸
- preload 之后的直接瓶颈：高召回区间的 CPU 搜索成本

## 风险与缓解措施
- 风险：CPU 侧调优也无法足够推动高召回区间。
  - 缓解：停止继续正面追击 DiskANN 的暖延迟，将主声明转到 IVF 家族领先性与构建/启动优势。
- 风险：如果 DiskANN 使用 FlatStor 而其他系统使用另一后端，评审人质疑公平性。
  - 缓解：要么在层 A 中对齐后端，要么明确标记 DiskANN+FlatStor 为上界参考，并分开展示后端消融。
- 风险：基线空间变得太大无法干净执行。
  - 缓解：冻结分层基线策略并拒绝完整笛卡尔扩展。
- 风险：`Parquet` 成为工程时间的消耗而无助于改变结论。
  - 缓解：只在 `FlatStor` vs `Lance` 稳定后才添加 `Parquet`。
- 风险：真实数据集引入 schema 和 payload 异质性，拖慢主故事收敛。
  - 缓解：等 COCO + Deep 的流程稳定后再上真实数据。

## 最终检查清单
- [x] 主论文表格已覆盖
- [x] DiskANN 作为强基线保留在范围内
- [x] 搜索核心和存储后端效应已分离
- [x] 可选运行与必须运行已分离
- [x] 冷启动仍明确排除在范围外
